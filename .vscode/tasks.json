{
    "version": "2.0.0",
    "tasks":
    [
        {
            "label": "python3: run this file locally",
            "type": "shell",
            "command": "cd ${fileDirname} && python3 -u ${file}",
            "group": "build",
        },
    ]
}